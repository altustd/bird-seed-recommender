# No API keys required for core use of the Backyard Seed Advisor.
# (Optional future: CENSUS or eBird keys could go here for "what's being seen near me" features;
#  those would be loaded via macOS Keychain or python-dotenv in a future version.)
#
# Required for this project: none (fully static data)